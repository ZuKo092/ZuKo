{
    "name": "anfragebox/anfragebox",
    "description": "AnfrageBox - Lead management SaaS for moving companies",
    "type": "project",
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "autoload": {
        "files": [
            "anfrage/api/config.php"
        ]
    },
    "scripts": {
        "test": "phpunit",
        "lint": "find . -name '*.php' -not -path './vendor/*' -exec php -l {} \\;"
    }
}
